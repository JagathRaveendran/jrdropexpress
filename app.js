function redirectToPage(page) {
    // Redirect to the specified page
    window.location.href = page;
}
function home(page) {
    // Redirect to the specified page
    window.location.href = page;
}
function watches(page) {
    // Redirect to the specified page
    window.location.href = page;
}